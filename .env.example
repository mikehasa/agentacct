# Optional: only required for explicit provider forwarding or paid judge experiments.
# Use low-limit test keys. Do not commit real keys.
# Compat: the pre-rename AGENT_SENTINEL_* names are still accepted (silently);
# when both are set, the AGENT_CHRONICLE_* name wins.
AGENT_CHRONICLE_OPENROUTER_API_KEY=
AGENT_CHRONICLE_OPENAI_API_KEY=
AGENT_CHRONICLE_DEEPSEEK_API_KEY=
