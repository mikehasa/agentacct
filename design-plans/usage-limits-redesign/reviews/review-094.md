## Scenario

094 — Pointer user scans without explanatory prose.

## Verdict

The decision-first hierarchy is strong.

## Findings

Capacity now, explicit provider window/reset, and selected-range use answer the core question in one row. Moving calibration and definitions into About reduces prose overhead. Scanability still depends on putting named absence and threshold severity directly in rows, not only in the disclosure.

## Recommendation

Keep column headers persistent, use short inline states (“limit not reported,” “critical”), and reserve About for methodology rather than facts needed to act.

## Test idea

Five-second test the populated, usage-only, and stale variants; ask users to identify the riskiest client and why without opening About.
